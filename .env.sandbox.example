# BIMWeb sandbox / dev environment — copy to .env.local or .env.sandbox.local
# Uses Neon `dev` branch. Never commit files with real credentials.

ENV=sandbox

# Neon (sandbox/dev branch — see docs/environments.md)
NEON_PROJECT_ID=your_neon_project_id
NEON_BRANCH_ID=your_neon_dev_branch_id
DATABASE_URL="postgresql://user:pass@host/neondb?sslmode=require"

# Kinde (sandbox app or localhost callbacks)
KINDE_ISSUER_URL=https://your-subdomain.kinde.com
KINDE_CLIENT_ID=your_kinde_client_id
KINDE_CLIENT_SECRET=your_kinde_client_secret
KINDE_SITE_URL=http://localhost:3000
KINDE_POST_LOGOUT_REDIRECT_URL=http://localhost:3000
KINDE_POST_LOGIN_REDIRECT_URL=http://localhost:3000/dashboard

# Local backend services (docker compose or start-platform.sh)
NEXT_PUBLIC_BIMAGENT_URL=http://localhost:8000
NEXT_PUBLIC_BIMINDEX_URL=http://localhost:8001
NEXT_PUBLIC_BIMEXTRACT_URL=http://localhost:8200
NEXT_PUBLIC_BIMCLOUD_URL=http://localhost:8080
NEXT_PUBLIC_APP_URL=http://localhost:3000

BIMAGENT_URL=http://127.0.0.1:8000
BIMINDEX_URL=http://127.0.0.1:8001
BIMEXTRACT_URL=http://127.0.0.1:8200
BIMCLOUD_URL=http://127.0.0.1:8080

# E2E / Playwright (sandbox and CI only — never enable in production)
E2E_TEST_BYPASS=true
E2E_TEST_USER_ID=kinde_test_user_id
E2E_TEST_USER_EMAIL=e2e-test@example.com
ECOSYSTEM_E2E=true

STORAGE_BACKEND=local

# Optional: Vercel preview alignment (set in Vercel dashboard for Preview env)
# VERCEL_ENV=preview
